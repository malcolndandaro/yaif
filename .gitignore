# Databricks Asset Bundle state
.databricks/
.bundle/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-test/
venv/
.ipynb_checkpoints/
.pytest_cache/

# Generated domain YAML preview dir (scripts/generate_api_domains.py default out-dir)
build/

# IDE / OS / local AI-tooling state (vendored skill copies, per-machine config)
.vscode/
.idea/
.DS_Store
.claude/
.cursor/
.isaac/

# Engagement-specific artifacts — not part of the reusable framework
reports/

# Design-tooling scratch: verification screenshots, downloaded craft references,
# decision-page logs. DESIGN.md's machine-readable sidecar IS tracked, because the
# design detector reads it to validate the palette and type ramp.
.impeccable/*
!.impeccable/design.json

# Secrets / customer-provided artifacts — NEVER commit (may contain plaintext creds)
# e.g. exported API client collections often embed usernames/passwords/tokens
*.postman_collection.json
*.postman_environment.json
secrets/
*.secrets.*
*credentials*.json
